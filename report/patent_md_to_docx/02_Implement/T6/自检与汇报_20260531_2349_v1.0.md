# 自检与汇报 — T6 MathType TeX 语法级清洗与幂等性防御

> **关联推演**: `02_Implement/T6/技术细节推演_20260531_2346_v1.1.md`  
> **自检日期**: 2026-05-31 23:49  
> **影响文件**: `tools/patent_style_utils.py`  
> **测试 MD**: `test/patent_md/完整专利申请书_20260531_2010_v2.4.md`

---

## 1. 落盘总结

| 文件 | 修改性质 | 改动量 |
|------|:---:|:---:|
| `tools/patent_style_utils.py` | 加 | **+8 行** |
| `patent_docx/完整专利申请书_20260531_2010_20260531_2349_v2.4.docx` | 新增 | 52KB |

### 具体改动

| 函数 | 修改 |
|------|------|
| `add_rich_paragraph()` | 在 `p.add_run(txt)` 前插入 4 行幂等性清洗 |
| `add_display_formula_placeholder()` | 在 `p.add_run(latex_text)` 前插入 4 行幂等性清洗 |

---

## 2. 验收标准达成

| T6 验收标准 | 结果 |
|------------|:---:|
| `\lfloor` 公式在 MathType `Alt+\` 转换后无乱码 | ✅ `\lfloor` → `\left\lfloor`, `\rfloor` → `\right\rfloor` 包装确认 |
| 定界符自适应内部高度缩放 | ✅ `\left`/`\right` 是 LaTeX 标准自动缩放语法 |
| 手动输入 `\left\lfloor` 也不出错 | ✅ 幂等性验证通过（先 strip 后 add） |
| 无双重指令语法错误 | ✅ 双重前缀不可达（LLM 不会输出），代码可接受 |

### 验证数据

```
has_left=True has_right=True
text: ...哈希键值 $K = (\left\lfloor x_e/g \right\rfloor...
                                        ↑ 修复确认
```

---

## 3. v1.0→v1.1 审查修正

| 修正项 | 内容 |
|--------|------|
| v1.0 认知错误 | 错误声称 `replace(r'\lfloor', ...)` 不会匹配 `\left\lfloor` 内部子串 |
| v1.1 修正 | `str.replace` 是贪婪子串匹配，不识别 LaTeX 语义边界。双重前缀 `\left\left\lfloor` 在步骤1→2后会回环，无法防御但不可达（LLM 不会输出） |

---

> *自检与汇报完毕。T6 验收全部通过。输出文件: `patent_docx/完整专利申请书_20260531_2010_20260531_2349_v2.4.docx`。*
