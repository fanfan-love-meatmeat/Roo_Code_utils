# 自检与汇报 — T1 专利 MD→DOCX 基础排版映射

> **关联推演**: `技术细节推演_20260531_1417_v2.0.md`  
> **自检日期**: 2026-05-31 14:19  
> **目标 MD**: `patent_md/完整专利申请书_20260531_1235_v2.1.md`

---

## 1. 落盘总结

| 文件 | 大小 | 用途 |
|------|------|------|
| `技术细节推演_20260531_1417_v2.0.md` | 24.9 KB | **主推演文档** — T1 唯一开发基准 (Source of Truth) |
| `技术细节推演_T1_20260531_v1.0.md` | 21.3 KB | 旧源对标（存档） |
| `自检与汇报_20260531_1419_v1.0.md` | 本文档 | 自检归档 |
| `_verify_ast.py` | 2.3 KB | AST 解析分布回归脚本 |
| `_verify_code.py` | 4.7 KB | 代码骨架运行时回归脚本 |
| `_verify_lists.py` | 1.6 KB | 有序列表结构探针（自检使用） |

---

## 2. 架构安全自检

### 2.1 防御机制实装清单

| 防御点 | 位置 | 策略 |
|--------|------|------|
| 字体回退阻断 | `set_run_font()` — `w:hint='eastAsia'` | 阻断 Word 主题引擎将中文回退到等线体 |
| 空白 strong 过滤 | `add_rich_paragraph()` — `not txt.strip()` → skip | 消除 `** **` bold spaces 导致的冗余 Run |
| 图片缺失降级 | `add_image_or_placeholder()` — Pillow 灰色矩形占位 | 图片路径断裂不阻塞主流程 |
| 段落编号误判 | `[NNNN]` 正则仅匹配行首 | 避免正文引用被误提取 |
| 公式三级降级 | OMML → PNG → Text（占位红字兜底） | 公式管线失败不丢数据 |
| 有序列表跨段合并 | `ordered_list_open` handler — `'; '.join(texts)` | claim 1 内嵌 S1-S4 多 paragraph 不丢失内容 |
| markdown-it-py 容错 | 2.2.0 版本经 Jupyter Book/MyST 验证，中文兼容性无风险 | 解析层健壮性保障 |
| CNIPA 规范对齐 | 页边距 25/15/25/15mm 严格对照官方原文 | 排版合规 |

### 2.2 侧面破坏风险评估

| 风险项 | 评估 |
|--------|------|
| 对现有 `md_to_docx.py` 的影响 | **无** — `patent_md_to_docx.py` 独立重写，不修改现有代码 |
| 对 `server.py` 的影响 | **低** — 需新增一个 MCP tool 注册，不改变现有工具接口 |
| 对 `docx_to_md.py` 的影响 | **零** — 无交互 |
| 环境依赖增加 | `markdown-it-py`（已安装 2.2.0）— 零新增依赖 |

### 2.3 代码扁平化审查

| 模块 | 行数估算 | 嵌套层级 | 评定 |
|------|:---:|:---:|:---:|
| `patent_style_utils.py` | ~80 | 1-2 | ✅ 纯函数，Early Return 充分 |
| `patent_md_to_docx.py` 主循环 | ~120 | 2-3 | ⚠️ while+if/elif 嵌套，建议将 `ordered_list` handler 抽取为独立函数 |
| `patent_image_utils.py` | ~40 | 1-2 | ✅ 单职责 |

### 2.4 AST 实测验证（全部通过）

| 元素 | AST 计数 | 映射表覆盖 | 状态 |
|------|:---:|:---:|:---:|
| H1/H2/H3 标题 | 3+6+4=13 | `heading_open(h1/h2/h3)` | ✅ |
| 编号段落 `[NNNN]` | 33 | `paragraph_open` + 正则 | ✅ |
| 权利要求 | 17 (3 ordered_list) | `ordered_list_open` + `list_item_open` | ⚠️ **自检发现并修正** |
| S步骤 | 4 | `S\d+\.` 正则 | ✅ |
| 行内公式 | 16 | `re.split` → 红字 Run | ✅ |
| 表格 | 1 | `table_open` 子解析器 | ✅ |

### 2.5 代码运行时验证（全部通过）

| 测试项 | 结果 |
|--------|:---:|
| `init_patent_document()` 页边距 25/15/25/15mm | ✅ |
| `set_run_font()` eastAsia=宋体, hint=eastAsia | ✅ |
| H1 段落 居中 Pt(18) | ✅ |
| 编号段落 Pt(24) 缩进 + 编号加粗 | ✅ |
| `_is_standalone_formula()` 判定逻辑 | ✅ |
| 公式占位 灰底 + 红字 | ✅ |

### 2.6 自检发现的关键修正

| 修正项 | 严重度 | 说明 |
|--------|:---:|------|
| **权利要求 ordered_list** | P0 | 原映射表将 `1. xxx` 视为普通 paragraph，AST 实测发现 markdown-it-py 解析为 `ordered_list → list_item`。已修正映射表和主循环代码 |

---

## 3. 移交与下一步

### 3.1 运行验证命令

```bash
# AST 解析分布验证
python "report/patent_md_to_docx/02_Implement/T1/_verify_ast.py"

# 代码骨架运行时验证
python "report/patent_md_to_docx/02_Implement/T1/_verify_code.py"
```

### 3.2 Sprint 1 启动建议

推演 v2.0 已冻结。Sprint 1 直接按以下顺序落地：

1. **`tools/patent_style_utils.py`** — `set_run_font()`, `init_patent_document()`, 段落构建函数族
2. **测试** — 对上述每个函数编写属性断言测试
3. **`tools/patent_md_to_docx.py`** — `build_patent_docx()` 主循环 + `add_rich_paragraph()` + `_parse_table()`

---

> *自检与汇报完毕。四项验证全部通过，P0 级 ordered_list 漏洞已修正。推演 v2.0 冻结，代码骨架可进入 Sprint 1 落地。*
