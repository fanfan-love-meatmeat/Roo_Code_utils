# 自检与汇报 — T4 H1 视觉边框重构与说明书附图自动挂载

> **关联推演**: `02_Implement/T4/技术细节推演_20260531_2240_v1.0.md`  
> **自检日期**: 2026-05-31 22:43  
> **影响文件**: `tools/patent_style_utils.py`, `tools/patent_md_to_docx.py`  
> **测试 MD**: `patent_md/完整专利申请书_20260531_2010_v2.4.md`

---

## 1. 落盘总结

| 文件 | 修改性质 | 改动量 |
|------|:---:|:---:|
| `tools/patent_style_utils.py` | 改 | **-6 / +3 行** |
| `tools/patent_md_to_docx.py` | 加 | **+7 行** |
| `patent_docx/完整专利申请书_20260531_2243_v2.5.docx` | 新增 | 52KB |

### 具体改动

| 位置 | 修改 |
|------|------|
| `add_section_title()` | 删除 `w:top` 循环 → 仅 `w:bottom` + `w:sz` 4→18 + `re.sub(r'\s+', '', clean)` 暴力清空 |
| `build_patent_docx()` 初始化 | 新增 `has_drawing_title = False` 标记 |
| `build_patent_docx()` heading_open 分支 | 新增 `if text == '说明书附图': has_drawing_title = True` |
| `build_patent_docx()` return 前 | 新增 `if not has_drawing_title: doc.add_page_break(); add_section_title(doc, "说明书附图")` |

---

## 2. 验收标准达成

| T4 验收标准 | 结果 |
|------------|:---:|
| H1 仅有底部边框，线条变粗 | ✅ 4个H1全部 `top=False, bottom=True, bot_sz=18` (2.25pt) |
| H1 文字绝对呈现 3 空格，无粘连 | ✅ `re.sub(r'\s+', '', ...)` → `'   '.join()` 暴力重排验证通过 |
| 最后一页必有 `# 说 明 书 附 图` 大标题 | ✅ 自动追加于文档末尾，含分页符 + 粗下边框 |

### 验证数据

```
A1: H1 bottom-only border + thickened (sz=18)
  top=False bottom=True bot_sz=18 | 说   明   书   摘   要
  top=False bottom=True bot_sz=18 | 权   利   要   求   书
  top=False bottom=True bot_sz=18 | 说   明   书
  top=False bottom=True bot_sz=18 | 说   明   书   附   图  ← 自动追加

A2: Document tail
  ... last body paragraph ...
  [空行]
  说   明   书   附   图  (18pt, 下边框, 新页)
```

---

## 3. 架构安全自检

| 防御点 | 状态 |
|--------|:---:|
| `has_drawing_title` 去重 | ✅ 若 MD 已含 `# 说明书附图`，不会重复追加 |
| `\s+` 覆盖全角空格 | ✅ T3 的 `replace(' ', '')` 仅清 ASCII 空格，T4 的 `re.sub(r'\s+', '', ...)` 覆盖全角/制表符等所有 whitespace |
| `w:sz='18'` 兼容性 | ✅ OOXML 标准属性，跨 Word 版本一致 |
| 空 MD 容错 | ✅ `has_drawing_title` 初始为 False，空文档或仅有权利要求书的 MD 也会追加说明书附图页 |
| 侧面破坏 | ✅ `add_section_title` 签名未变，`patent_md_to_docx.py` 主逻辑未受影响 |

---

> *自检与汇报完毕。T4 三项验收全部通过。输出文件: `patent_docx/完整专利申请书_20260531_2243_v2.5.docx`。*
