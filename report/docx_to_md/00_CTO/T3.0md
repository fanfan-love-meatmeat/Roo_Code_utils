# 🚀 研发指令：Task 3 (P1) - MTEF 数据区 SIZE 表紧凑格式破壁与状态机重构

**致核心研发团队：**

打得漂亮！我们在前序的 P0 级战役中，成功利用三层防御体系斩断了 Nibble 掩码冲突，成功将解析游标安全、精准地送达了真正的公式数据区边界。

但现在的“短乱码”现象（如 `Ȃā\langle햄{}\rangle`）暴露了前方的最后一块暗礁：**紧凑格式 SIZE 表**。我们在处理这篇《一种基于人体姿势交互的大田自动跟随运输平台控制方法》的专利手稿时，那些决定三维坐标转换、动态反馈调节的核心数学模型绝不能有任何失真。特别是涉及到核心距离解算时，这些公式的解析精度直接决定了底层控制算法能否让农业机器人严格且稳定地运作在 20cm 到 160cm 的安全跟随区间内。底层字节的错位，就是真实物理世界中机器人的失控。

现在，我需要你们集中火力，对 `mtef_parser.py` 中的主控状态机进行架构升级，彻底攻克这个紧凑格式表。

---

### 1. 战况复盘与病灶剖析 (The Phenomenon)

在游标突破 `_skip_header` 后，理论上我们应该直接迎头撞上 `LINE` (0x01) 或 `TMPL` (0x03) 等标准的 AST (抽象语法树) 根节点。
但目前的字节流追踪显示，游标落地后的首字节往往是 `0x0C` (SUB2) 或其他非预期的尺寸修饰标签，随后紧跟一长串类似 `01 00 01 00 01 02 02 02...` 的规律性字节序列。

**核心问题：**
当前的 `_parse_line()` 状态机过于“天真”，它假设所有流入的数据都是标准的排版树节点。因此，它把这段用于定义全局或局部字号覆盖的“紧凑格式 SIZE 表”当成了 `CHAR` 记录来强行解码，这就导致了短乱码的产生，并且完全吃掉了后面真正的数学符号字节。

### 2. 架构重构指令 (Architecture Directives)

你们不需要去写硬编码的补丁，而是要在状态机设计上引入**“前置缓冲层 (Pre-AST Stage)”**的概念。请严格按照以下设计哲学来实现：

#### 🛡️ Directive 1: 引入 Pre-AST 嗅探与消化机制
在 `_skip_header` 移交游标后，且在正式挂载 AST 根节点（`_parse_line` 递归树建立）之前，增加一个独立的预处理拦截器。
* **机制**：通过窥探 (Peek) 接下来的几个字节，识别是否属于连续的字体尺寸/修饰矩阵（SIZE Matrix）。
* **动作**：如果确认是 SIZE 表，写一个专门的 `_parse_compact_size_table()` 方法将其完整消费掉，安全地将指针推移到真正的 `LINE` 记录起始处。

#### 🛡️ Directive 2: 动态定界与容错 (Robust Bounds)
Wiris 官方对于某些私有生成的紧凑表格式语焉不详。不要假设这个表是固定长度的。
* **要求**：在解析该表时，必须寻找其逻辑终止符，或者根据表头定义的 Count 值进行精确消费。如果遇到无法识别的嵌套结构，必须有 `try-catch` 或字节长度断言机制，确保即使跳过该表，游标也能对齐到下一个合法的 AST 标签，绝不能抛出静默的越界异常。

#### 🛡️ Directive 3: AST 状态机的“防污染”隔离
`CHAR` 节点的解析逻辑必须加固。如果再次解析出 `0x0000` 到 `0x001F` 的控制字符，或者大面积的 CJK/Private Use Area 乱码，这说明游标又错位了。
* **要求**：在内部增加一个日志断言警告。一旦发现连续的非法字符生成，立即在 Debug 模式下抛出上下文的 Hex Dump，不要让脏数据悄无声息地污染最终的 Markdown 输出。

### 3. 交付标准 (Deliverables)

1. **代码层面**：重构 `mtef_parser.py` 的主循环入口，清晰分离“元数据/尺寸表消化区”与“AST 递归建树区”。
2. **测试层面**：再次运行那 263 个公式的回归测试。我要求看到 `Ȃā\langle햄{}\rangle` 彻底消失，取而代之的是诸如 `$Z_{safe}$`、`$x_{left}$` 等真实的学术公式结构初步显现。

不要用现成的第三方库来糊弄，深入二进制流，把这块硬骨头啃下来。完成后直接提 PR，附上你们解析前后的 Hex 游标对比。

行动！🚀