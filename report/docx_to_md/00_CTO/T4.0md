# 🚀 研发指令：Task 4 - AST 空间拓扑重建与 TMPL 嵌套状态机重构

**致核心研发团队：**

我们在 Task 3 和 Task 3.1 连克两座高地，成功斩断了 Nibble 掩码冲突，并全面攻克了二进制字符映射。现在，所有的生僻字已经全部净化，`\Chi`、`\Delta`、`P`、`o` 这种纯净的原子级“砖块”已经完美浮出水面。

但正如最新一轮自检报告所揭示的：**我们的公式大厦现在被拍扁了**。
目前系统吐出的是 `\Chi c` 而非 `\Chi_{c}`，`\theta p` 而非 `\theta_{p}`。这意味着所有的上下标、分式、根号等空间拓扑关系，在状态机遍历时发生了**“维度塌缩”**。原稿中决定大田机器人控制算法、坐标转换矩阵以及 20cm 至 160cm 动态安全距离解算的精细数学拓扑，被错误地拉平成了一维的纯文本流水账。这在学术和专利审查层面上是不可接受的。

现在，我正式下发 **Task 4**，要求团队立即启动 **AST 空间拓扑重建战役**，攻克 `TMPL`（模板）节点的嵌套控制逻辑。

---

### 1. 战局剖析：TMPL 状态机的“失序”

在 MTEF v5 协议中，`TMPL` (0x03) 节点负责定义公式的空间骨架。
当游标读取到 `TMPL` 记录时，它不仅包含一个 `selector`（模板选择器，如上下标、分式、矩阵），还包含一个 `variation`（变体）以及后续紧跟的一个或多个 `LINE` (0x01) 子节点。

**目前的架构缺陷：**
我们现有的 `_parse_line` 状态机是一个一维的、扁平化的递归处理器。当它遇到 `TMPL` 节点时，仅仅是把它当成了一个普通的普通节点略过，然后继续去抓后面的 `CHAR`。它没有在逻辑上建立**“父子包含”**和**“作用域隔离”**的关系。
* 它知道后面有一个 `c` 字符。
* 但它不知道这个 `c` 字符必须被嵌套在它的父级——“下标模板（Subscript Template）”的控制域 `{}` 之内。

这就导致了空间排版树的全面坍缩。

---

### 2. 架构重构指令 (Architecture Directives)

本项任务由解析器内核小组全权负责。我要求你们废弃一维的拉平遍历，在 `mtef_parser.py` 中引入**拓扑上下文栈（Topology Context Stack）**。请遵循以下三个最高设计方针：

#### ⚙️ 指令一：实装 TMPL 作用域拦截与 LaTeX 骨架打桩 (Template-Driven Scoping)
必须重构 `TMPL` 节点的分支处理算子。
* **路由映射**：根据 `selector` 字节的真值，建立完整的排版骨架路由表。优先确保高频拓扑的覆盖：
    * `Subscript` (下标) $\rightarrow$ 拦截并打桩为 `_{...}`
    * `Superscript` (上标) $\rightarrow$ 拦截并打桩为 `^{...}`
    * `Fraction` (分式) $\rightarrow$ 拦截并打桩为 `\frac{...}{...}`
    * `Radical` (根号) $\rightarrow$ 拦截并打桩为 `\sqrt{...}`
* **上下文入栈**：当遭遇 `TMPL` 时，根据模板类型向拓扑栈压入一个环境上下文，强行改变后续 `LINE` 节点的文本拼装行为。

#### ⚙️ 指令二：攻克“多插槽（Multi-Slot）”子树消费逻辑
像 `Fraction`（分式）或 `SubSuper`（上下标同时存在）这样的复杂模板，在其内部会连续嵌套多个 `LINE` 节点（例如分式包含分子 `LINE` 与分母 `LINE`）。
* **状态机解耦**：状态机必须能够精准识别当前模板期望的“子树插槽数量”（Slot Count）。
* **多项隔离**：在遍历子 `LINE` 节点时，必须能够通过特定的控制字节隔离技术，将不同的子树结果精准分配到 LaTeX 的不同花括号中。例如分式模板必须生成 `\frac{分子分支结果}{分母分支结果}`，绝不允许发生多插槽数据合并或错位泄漏。

#### ⚙️ 指令三：拓扑定界与越界防卫 (Topological Edge Guard)
手敲公式的嵌套层级可能极其深邃（比如在大田控制算法中，存在多层下标叠加坐标系的极端情况）。
* **深度控制**：必须引入递归深度断言，防止因为异常的数据死循环导致解析器发生栈溢出（Stack Overflow）。
* **未知模板降级**：如果遭遇非标准的、或者极为偏门的 MathType 矩阵变体选择器，严禁抛出异常中断管线。必须降级为通用容器形式 `\boxed{...}`，将子节点内容包裹放行，确保整体 AST 的抗震能力。

---

### 3. 验收与交付指标 (Acceptance Criteria)

1.  **空间维度的完美复苏**：重新运行 263 个公式的回归测试。在最终的 Markdown 文件中，`\Chi c` 必须 100% 进化为 `\Chi_{c}`，`\theta p` 必须 100% 进化为 `\theta_{p}`。
2.  **核心算法公式链闭环**：S1 到 S6 步骤中的所有数学模型必须达到学术出版级精度。尤其是三维坐标转换公式、PID 动态反馈控制函数以及弯腰/举手动作的几何逻辑判断式，必须呈现出完美的上下标、分式及行列式拓扑。
3.  **零语法崩溃**：生成的 LaTeX 代码必须符合标准语法，能够被任何主流的 Markdown 渲染器直接原生解析，无缺失的花括号 `{` 或 `}`。

元数据和原子符号的战斗已经结束。现在，把空间结构给我建立起来，恢复这篇控制方法手稿的本源面貌！

期待你们提 PR，我去准备进行最终轮的 Code Review。

Go fight! 🚀
```